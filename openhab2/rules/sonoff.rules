val sonoff_device_ids = newArrayList("Sonoff")

rule "Sonoff Maintenance"
when
    Item Sonoff_Action received command
then 
    logInfo("sonoff.rules", "Sonoff Maintenance on all devices: " + receivedCommand)
    for (String device_id : sonoff_device_ids) {
        switch (receivedCommand) {
            case "restart" :
                publish("pri", "cmnd/" + device_id + "/restart", "1") 
            case "status" :
                publish("pri", "cmnd/" + device_id + "/status", "2")
            case "upgrade" : {
                publish("pri", "cmnd/" + device_id + "/otaurl", "http://thehackbox.org/tasmota/release/sonoff.bin")
                publish("pri", "cmnd/" + device_id + "/upgrade", "1")
            }
        }
    }
    Sonoff_Action.postUpdate(NULL)
end
