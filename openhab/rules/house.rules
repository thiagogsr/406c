rule "House actions"
when
    Item HouseAction received command
then
    logInfo("sonoff.rules", "House Action on all devices: " + receivedCommand)

    AllLights.members.forEach[i| i.sendCommand(OFF)]
    AllBroadlinks.members.forEach[i| i.sendCommand("off")]

    HouseAction.postUpdate(NULL)
end
